# local mkcert generated certificates for https
.certs
# never commit this
.env
# I'd like to commit this but probably not a good idea
.vscode
# compiled website
dist
# dependencies
node_modules
# made by justfile docker machinery
.tmp
